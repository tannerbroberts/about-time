{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@tannerbroberts/about-time-core && npm run build --workspace=@about-time/api-client && npm run build --workspace=@about-time/frontend"
  },
  "deploy": {
    "startCommand": "npx serve -s apps/frontend/dist -l $PORT",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
